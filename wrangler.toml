name = "lele-island"
pages_build_output_dir = "dist"
compatibility_date = "2025-05-01"

# D1 数据库（用于用户登录 + 云端存档）
# 创建: npx wrangler d1 create lele-island
# 把输出的 database_id 填到下方
[[d1_databases]]
binding = "DB"
database_name = "lele-island"
database_id = "231d737f-d792-45cb-83ac-84cdc0b71acb"

# 环境变量
[vars]
# DEEPSEEK_API_KEY 通过 npx wrangler pages secret put DEEPSEEK_API_KEY 设置
